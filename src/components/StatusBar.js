/**
 * STATUS BAR
 * - logo
 * - date range picker
 * - api status
 * - api selector
 */
import Logo from './cloki-logo.png'
export const StatusBar = (props) => {
return(
    <div>
        <img src={Logo}
        height="28px"
        />
    </div>
)
}